version: '3'

services: 
  ydl:
    image: ydl-sync
    container_name: ydl-sync
    restart: unless-stopped
    volumes: 
      - /resilio/sync/ydl/music2020:/download
      - /home/kitusne/docker/ydl/config:/etc/ydl-sync
